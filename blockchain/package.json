{
  "name": "secure-mailbox-blockchain",
  "version": "1.0.0",
  "description": "Hardhat blockchain component for the secure mailbox: MessageDigest anchoring, KeyRegistry, and MessageReceipt",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy-registry:sepolia": "hardhat run scripts/deploy-registry.js --network sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0",
    "solc": "^0.8.20"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
